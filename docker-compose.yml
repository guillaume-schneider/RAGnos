version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis-rag
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always
    command: redis-server --appendonly yes

volumes:
  redis_data:
